import React from 'react';
{{> importScssPartial}}


interface Props {
  // TODO: implement. for example:
  // children: React.ReactNode; // includes all node types like elements, string, null (also as arrays)
}

/**
 * {{description}}
 * @param {object} props
 * TODO:
 * 1. move to appropriate sub-folder
 * 2. implement the component
 * 3. fill TSDoc - description, props description and code examples if needed
 */
export const {{pascalCase name}}: React.FC<Props> = function {{pascalCase name}}(props: Props): React.ReactElement {
  return <>Implement...</>;
};
